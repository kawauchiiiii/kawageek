<h1>GeekTwitter</h1>
<h3>Tweet詳細</h3>
<div class="review">
<p><%= @review.title %></p>
  <p><%= @review.body %></p>
  <p><%= @review.created_at.to_s(:datetime_jp) %></p>
</div>

<%= link_to "Tweet一覧に戻る", reviews_index_path %>